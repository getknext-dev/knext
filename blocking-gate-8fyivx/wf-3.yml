
on:
  pull_request:
jobs:
  upstream:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo hi
  gate:
    needs: upstream
    runs-on: ubuntu-latest
    steps:
      - name: Run it
        run: run-the-gate
