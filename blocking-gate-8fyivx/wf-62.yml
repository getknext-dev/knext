concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on:
  pull_request:
jobs:
  gate:
    name: Gate
    runs-on: ubuntu-latest
    steps:
      - name: Run it
        run: run-the-gate
  other:
    name: Other
    runs-on: ubuntu-latest
    steps:
      - name: Something
        run: echo hi
