// GENERATED BY `turbo gen zone` — the knext app template (ADR-0031, #356).
//
// This file is the edge-clean half of the guarded-instrumentation pair every
// generated knext app ships with. It MUST stay EDGE-CLEAN: Next.js compiles
// `instrumentation.ts` for BOTH the `nodejs` AND the `edge` runtimes (adding a
// `middleware.ts` forces an edge build). All knext observability/db-wake wiring
// is Node-only by nature — `./instrumentation-node` reaches `@knext/lib/clients`
// (→ `@cerbos/grpc` → `@grpc/grpc-js`, needing `zlib`/`stream`/`net`/`tls`/`fs`),
// plus `pg` and `minio`. A top-level static import of any of that lands in the
// edge bundle and the production `next build` fails with `Module not found`
// (#342).
//
// The Node-only body therefore lives in `./instrumentation-node.ts`, loaded via
// a dynamic `await import(...)` and CALLED only when `NEXT_RUNTIME === 'nodejs'`
// (the runtime guard below). NOTE (#344): the dynamic import uses a STATIC
// string literal, so webpack STILL traces that module (and its Node-only
// subtree) into BOTH runtime bundles — the runtime guard stops it EXECUTING on
// the edge, NOT from being BUNDLED. The LOAD-BEARING edge exclusion is the
// `IgnorePlugin` the knext adapter injects from its `modifyConfig`
// (`./next-adapter.ts` → `@knext/core/adapter`, #356/ADR-0031): for the edge
// compile ONLY it replaces `./instrumentation-node` with an empty module, so
// app authors never hand-write that webpack hook. On the edge runtime
// `register()` is a no-op anyway — the knext runtime runs the app on Node (the
// standalone server), so nothing is lost.
//
// The shipped guard `instrumentation-edge-safe.test.ts` (generated alongside
// this file) enforces BOTH halves of the fence: this file never regains a
// top-level Node-only import, AND the adapter wiring stays in place. That class
// must fail the gate, not the deploy build — #342/#344/#356.
export async function register() {
  if (process.env.NEXT_RUNTIME !== 'nodejs') {
    return;
  }

  // Static-literal dynamic import: webpack bundles `./instrumentation-node` into
  // the NODEJS instrumentation chunk (so it works at runtime). webpack traces
  // this literal specifier into the edge compile TOO; the adapter-injected
  // edge-scoped `IgnorePlugin` is what replaces it with an empty module there so
  // its Node-only client subtree never enters the edge bundle — #342/#356.
  const { registerNode } = await import('./instrumentation-node');
  registerNode();
}
