/**
 * GENERATED BY `turbo gen zone` — the knext app template (ADR-0031, #356).
 *
 * Thin re-export of the package-shipped knext NextAdapter, wired via the
 * top-level `adapterPath` in `next.config.ts`. The adapter:
 *  - forces `output: 'standalone'` at production build, and
 *  - injects the edge-scoped webpack `IgnorePlugin` that excludes
 *    `src/instrumentation-node.ts` from the EDGE bundle (#342/#344) from its
 *    `modifyConfig` — so this app NEVER hand-writes that webpack hook.
 */

import adapter from '@knext/core/adapter';

export default adapter;
