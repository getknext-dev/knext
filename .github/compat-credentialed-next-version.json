{
  "$comment": "Documents a DELIBERATE divergence (#1376): test-e2e-deploy.yml's NEXTJS_REF (the Next.js ref every compat cell tests) currently differs from packages/kn-next/templates/app/package.json.hbs's next pin (the Next.js version the scaffold actually ships). tests/nextjs-credential-lockstep.test.ts fails if either real value drifts from what THIS file documents, so a future bump to either side must update this manifest in the same PR — a reviewed decision, never silent drift.",
  "credentialedNextRef": "v16.2.0",
  "shippedNextPin": "16.3.3",
  "divergenceReason": "Next 16.3.0 introduced a confirmed upstream regression (Turbopack + adapterPath + output:'standalone' -> ENOENT next-server.js.nft.json, vercel/next.js; see issue #1372) that breaks every knext harness deploy under Turbopack. Bumping the credential to the shipped pin today would zero out both turbopack-credentialed lanes (node x turbopack, bun x turbopack) with no fix available. Tracked in issue #1376 pending an upstream fix or a knext-side workaround.",
  "lastReviewed": "2026-09-25",
  "lockstepExceptions": [
    {
      "file": ".github/workflows/compat-shipped-pin-early-warning.yml",
      "kind": "env-fallback",
      "value": "v16.3.3",
      "reason": "The #1376 option-(b) shipped-pin early-warning lane (founder-approved 2026-09-25): a non-credential, informational-only lane that tests the SHIPPED pin (currently v16.3.3), not the credentialed v16.2.0 — by design, since its whole point is forecasting what the shipped pin looks like on the real compat suite. It reads shippedNextPin from THIS manifest at dispatch time (scripts/lib/dispatch-poll.mjs's shippedPinRef, never a hardcoded literal in the workflow), so this entry documents the intended divergence rather than excusing a static drift the round-3 scan would otherwise catch — tests/compat-shipped-pin-early-warning.test.ts proves the tie directly. Every dispatched run runs via workflow_dispatch (never schedule), so KNEXT_COMPAT_MODE in test-e2e-deploy.yml is unconditionally early-warning and this lane can never advance a v1.0 credential count."
    }
  ]
}
