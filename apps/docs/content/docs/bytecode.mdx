---
title: Bytecode caching
description: Skipping JS recompilation on cold pods — NODE_COMPILE_CACHE on Node, per-file .jsc precompilation on Bun.
---

Scale-to-zero means cold starts. The expensive part of a Next.js cold start is the JS engine
compiling JavaScript from source. knext skips that work: on **Node** by persisting the V8 bytecode
cache to a volume, and on **Bun** by precompiling the standalone tree to bytecode at **build
time** (see [Bun runtime](/docs/bun-runtime)). This page covers the Node mechanism.

<Callout type="warn">
**Opt in deliberately — this caps how wide your app can scale.** The Node bytecode cache is off by
default, and it is not a free win. The cache lives on a persistent volume that **attaches to one
node at a time**, so if your app scales out and Kubernetes places a pod on a *second* node, that
pod cannot attach the volume and will sit **Pending** instead of serving traffic. On a cluster
with **no default storage class**, the volume never binds at all and the app does not start.

Enable it when cold-start latency matters more than horizontal fan-out — a read-mostly zone that
wakes from zero and runs a handful of pods on one node. Leave it off for anything that bursts
wide. Confirm your cluster has a default storage class before turning it on.
</Callout>

## How it works (Node)

knext runs the standalone server with `NODE_COMPILE_CACHE` pointing at a directory. On the first
start V8 serializes compiled bytecode to that directory; every later cold pod reads it and skips JS
parsing/compilation.

The runtime image starts the server like this:

```dockerfile
# NODE_COMPILE_CACHE: V8 bytecode cache for faster subsequent cold starts.
CMD ["sh", "-c", "export NODE_COMPILE_CACHE=\"${NODE_COMPILE_CACHE:-.next/compile-cache}\"; exec node server.js"]
```

The Dockerfile supplies a fallback path; the operator's externally-injected value wins. When
`spec.cache.enableBytecodeCache: true`, the operator injects:

```
NODE_COMPILE_CACHE=/cache/bytecode/latest
```

backed by a persistent volume, so the cache survives pod death and is shared across cold starts.

This is independent of your **data cache**. Bytecode caching governs how fast the server *boots*;
the cache provider (`redis`, and friends) governs where *ISR and data* are cached. You can turn on
bytecode caching with no data-cache provider configured at all, and vice versa.

<Callout type="warn">
**Persistence is the whole point.** A pod-local cache directory would be re-primed on every cold
start and save nothing. The cache must live on a PVC (`enableBytecodeCache`) to pay off across pods.
</Callout>

## Node vs Bun

Both runtimes get a compile cache — via different mechanisms:

| Runtime | Bytecode mechanism |
|---|---|
| **Node** (default) | `NODE_COMPILE_CACHE` — V8 serializes bytecode to the PVC at runtime; every later cold pod reads it. |
| **Bun** (`runtime: 'bun'`) | **Build-time per-file bytecode precompilation**: `kn-next build` emits a companion `.jsc` for every server-side file (measured −47% startup, 287 ms → 152 ms median), plus Bun's runtime transpiler cache (`BUN_RUNTIME_TRANSPILER_CACHE_PATH`) on the same PVC. See [Bun runtime](/docs/bun-runtime). |

<Callout type="warn">
A Bun bytecode-built image is **Bun-only** — booting it with Node exits 1 with a `FATAL` message
naming the fix rather than crash-looping silently. Details and costs are on the
[Bun runtime](/docs/bun-runtime) page.
</Callout>
