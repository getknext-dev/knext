---
title: "Tutorial: deploy ACME"
description: Build ACME — a small Next.js storefront — from an empty directory to a scale-to-zero Knative service with a Postgres database, and watch it sleep and wake.
---

You are going to deploy **ACME**, a small storefront, and watch it fall asleep when nobody is using
it and wake up when somebody is. Then you will give it a database and see how that changes the
picture.

ACME is the same example used across [Getting started](/docs/getting-started), the
[CLI reference](/docs/cli) and the rest of these docs, so anything you learn here transfers.

**You do not need to be a Kubernetes expert.** You do need a cluster to deploy to — the section
after next is honest about how to get one.

## New to Kubernetes? Six words, then we start

Every term you meet below, in one line each. Skip this if they are already familiar.

| Word | What it means here |
|---|---|
| **Cluster** | A pool of machines that runs your containers. You rent one from a cloud, or run a small one locally. |
| **Pod** | One running copy of your app. Scale-to-zero means the number of pods can drop to **none**. |
| **Knative** | Software installed *into* the cluster that starts and stops pods for you based on traffic. It is what makes zero possible. |
| **Operator** | A program running in the cluster that watches for your instructions and carries them out. knext ships one. |
| **Custom resource** | Your instructions, as a file. You write "I want ACME, scaled to zero"; the operator makes it so. |
| **Ready** | The cluster's own word for "this is actually serving traffic". Not "deployed" — *working*. |

The shape of the whole thing: **you describe what you want, the operator makes it true.** You will
never create a Knative Service by hand in this tutorial, because the CLI does not either — it hands
the operator a `NextApp` resource and the operator owns everything downstream of that.

## What you need first

This tutorial deploys to a **real cluster**. Three things must exist before step 1, and none of them
is knext:

1. **A Kubernetes cluster with Knative Serving**, and the knext operator installed on it — see
   [Install](/docs/install). Without Knative there is nothing to scale to zero. Any cluster works:
   a managed one (GKE, EKS, AKS, OKE) or a local one.
2. **A container registry** you can push to, and that your cluster can pull from.
3. **An object-storage bucket** on `gcs`, `s3`, `minio`, or `azure`, with that provider's CLI
   (`gsutil` / `aws` / `mc` / `az`) installed **and authenticated** where you run the build. knext
   shells out to it to upload your static files.

<Callout type="warn">
There is no local-only path in this tutorial. Everything below assumes a cluster you can reach with
`kubectl`. If `kubectl get ksvc` fails, stop and fix that first — every later step depends on it.
</Callout>

If that list looks like a lot: it is the standing cost of self-hosting. You are buying no vendor
lock-in with it, and you pay it once per cluster, not once per app.

## 1 · Create the Next.js app

Nothing knext-specific yet. Any Next.js 16 app works; this is just to have something real to deploy.

```bash title="shell"
npx create-next-app@latest acme --ts --app
cd acme
```

## 2 · Add knext

```bash title="shell"
npm i @getknext/core
npx kn-next --version
```

`@getknext/core` is the whole deploy path: the official Next.js adapter, the typed config, and the
`kn-next` CLI. The CLI arrives on your `PATH` through `node_modules/.bin`, so `npx kn-next` works
without a global install.

## 3 · Point Next.js at the adapter

knext is a **Deployment Adapter** on Next.js's official API — not a fork and not a custom runtime.
One line tells Next.js to use it:

```ts title="next.config.ts"
import type { NextConfig } from 'next';

const config: NextConfig = {
  adapterPath: '@getknext/core/adapter',
};

export default config;
```

The adapter sets `output: 'standalone'` for you at build time; you do not need to add it.

<Callout type="info">
On Next.js 16.0.x–16.1.x the option lives under `experimental`
(`experimental: { adapterPath: '@getknext/core/adapter' }`). 16.2+ accepts it at the top level.
</Callout>

## 4 · Add the health route

**Do not skip this one.** The operator points Knative's readiness *and* liveness probes at
`/api/health` by default. `create-next-app` does not generate that route, so without it the probe
404s, the revision never becomes Ready, and step 8 will report `Ready=False` forever with nothing
obviously wrong in your app.

```ts title="app/api/health/route.ts"
export const dynamic = 'force-dynamic';

export function GET() {
  return Response.json({ status: 'ok' });
}
```

That is enough to go Ready. For a real app you want a health check that reports on its dependencies
too — `@getknext/lib` ships `checkShallowHealth()` and `checkDeepHealth()` for exactly that.

<Callout type="info">
Prefer a different path? Set `healthCheckPath` in the config below and the operator points the
probes there instead.
</Callout>

## 5 · Describe the deploy

Create `kn-next.config.ts` next to `next.config.ts`. Only three things are required — `name`,
`registry`, and a `storage` block with `provider`, `bucket`, and `publicUrl`:

```ts title="kn-next.config.ts"
import type { KnativeNextConfig } from '@getknext/core';

const config: KnativeNextConfig = {
  name: 'acme',
  registry: 'registry.example.com/acme',
  storage: {
    provider: 'gcs',
    bucket: 'acme-assets',
    publicUrl: 'https://storage.googleapis.com/acme-assets',
  },
  scaling: { minScale: 0, maxScale: 10 },
};

export default config;
```

`minScale: 0` is the line that makes this a scale-to-zero deployment. Set it to `1` and you have an
ordinary always-on service — useful to know, because it is the single knob that changes the whole
character of the deployment.

Substitute your own registry, bucket and `publicUrl`. The values above are placeholders and will
fail.

## 6 · Preflight before you deploy

```bash title="shell"
npx kn-next doctor
```

`doctor` is read-only. It checks the things that make a first deploy fail for reasons that have
nothing to do with your app: the CRD is present, the operator is Ready, Knative Serving is
installed, the ingress class matches the controller actually serving it, and your local `kubectl` is
recent enough. Fix anything it reports before deploying — the failures it catches are much easier to
read here than they are halfway through a build.

## 7 · Deploy

```bash title="shell"
npx kn-next
```

That one command builds the image, pushes it, uploads static assets to your bucket, and applies a
`NextApp` custom resource to the cluster. The **operator** does the rest — it owns every Knative
object, so the CLI never creates a Service or Route itself.

Expect the first run to be slow: it is a full container build.

## 8 · Check it is up

```bash title="shell"
npx kn-next status
```

This prints the app's conditions and its URL. `status` reports what the cluster actually says — if
something is wrong it tells you so rather than showing a green tick, so read it rather than assuming.

Then hit the URL it gave you:

```bash title="shell"
curl -sSf <the-URL-status-printed>/ > /dev/null && echo OK
```

## 9 · Watch it scale to zero — the part worth seeing

This is what you came for. Leave the app alone and watch its pods:

```bash title="shell"
kubectl get pods -l serving.knative.dev/service=acme -w
```

Give it about **90 seconds** — Knative waits out a 60s stable window and then a 30s grace period
before terminating the pod, so a user watching for "about a minute" concludes it is broken. Then the
list empties: no pod, no container compute for this app. (Your *nodes* keep running and billing
unless a cluster autoscaler independently reclaims them — scale-to-zero reclaims the pod, not the
machine.)

Now make one request:

```bash title="shell"
curl -sSf <the-URL-status-printed>/ > /dev/null && echo "awake"
```

Watch the pod reappear in the other terminal. Knative queued your request, started the pod, and
served it — normally you just get a slower response rather than an error. It is not a guarantee: a
cold wake can still return 503 if the revision or the readiness probe does not come up in time, and
the `-sSf` above will exit non-zero if that happens.

<Callout type="info">
**That first request after idle pays a cold start,** and how long it takes depends on your cluster,
your node sizing, whether the image is already cached on the node, and the app itself. We publish
measured distributions rather than a single headline number, because on a contended cluster the
spread is wide enough that a median would mislead you. See [Scale to zero](/docs/scale-to-zero).
</Callout>

## 10 · Give ACME a database

A storefront needs to remember things. This is where scale-to-zero gets interesting, because **an
app that sleeps has to reconnect every time it wakes.**

knext **provisions nothing**. It is engine-agnostic by design: you bring a Postgres — managed,
serverless, or a CloudNativePG cluster in the same Kubernetes cluster — and knext binds its
connection string into your pods. Swapping providers later is a change to one Secret, with no change
to your app or your config.

**First, put the connection string in a Kubernetes Secret.** A Secret is just a named bag of values
the cluster holds for you, so the DSN never sits in your repo or your image:

```bash title="shell"
kubectl create secret generic acme-db \
  --from-literal=DATABASE_URL='postgres://user:pass@host:5432/acme'
```

**Then reference it by name.** Add a `database` block to `kn-next.config.ts`:

```ts title="kn-next.config.ts"
const config: KnativeNextConfig = {
  name: 'acme',
  // …registry, storage, scaling as above…
  database: {
    secretRef: { name: 'acme-db' },
  },
};
```

Redeploy with `npx kn-next` and your pods come up with `DATABASE_URL` in their environment. Note
what you did *not* do: you never put the password in a file, and the app never learned which
provider it is talking to.

<Callout type="info">
Reads and writes can be split. Add `roSecretRef` alongside `secretRef` and knext binds a second
connection string as `DATABASE_URL_RO`, so bounded-stale reads can go to a replica. One Secret
carrying both keys can serve both fields. See [Databases](/docs/databases).
</Callout>

### The part that surprises people

Scale-to-zero and databases interact, and the interaction is the thing worth understanding:

- **Every cold start is a fresh connection.** A pod that was terminated took its connection pool
  with it. Put a **connection pooler** (PgBouncer, pgcat) between ACME and Postgres, and point the
  DSN at the pooler rather than the primary — otherwise a burst of waking pods opens a burst of
  connections and the database, not the app, becomes the bottleneck.
- **If your database also sleeps** — many serverless Postgres providers scale to zero too — then the
  first request after an idle period wakes *both*. Set the client's connect timeout comfortably
  above your database's own cold-wake time, or the app gives up before the database has finished
  waking and reports an error that looks like a database fault but is a timeout you chose.
- **A burst of cold pods shares one wake.** The pool single-flights the 0→1 database wake: the first
  request triggers it and the rest wait on the same wake rather than each starting their own.

[Databases](/docs/databases) has the pooler settings, the timeout guidance, and the typed
`@getknext/db` client for querying with a writer/reader split.

## 11 · When something goes wrong

| Symptom | First thing to run |
|---|---|
| Deploy fails before the build | `npx kn-next doctor` |
| Deploy succeeds, app never becomes Ready | `npx kn-next status` — it reports the real condition |
| Apply is rejected for an unknown field | Your operator is probably older than your CLI. Upgrade the operator first, then the CLI — see [Upgrading](/docs/upgrading) |
| Assets 404 in the browser | Check `storage.publicUrl` matches where the bucket actually serves, and that the provider CLI was authenticated during the build |
| Database errors only on the first request after idle | A cold-wake timeout, not a database fault — see step 10 |

## 12 · Clean up

```bash title="shell"
kubectl delete nextapp acme
```

Delete the `NextApp` and the operator removes what it created — and read the next sentence before
you run this against anything you care about.

<Callout type="warn">
**Deleting the `NextApp` also reaps app-scoped external state.** A finalizer clears the object-store
keys under this app's own `<app-name>/` prefix, and the Redis keys under its cache key prefix. If you
have parked anything else under that prefix in the same bucket, it goes too. **Registry images are
not touched** — those remain yours, and **your database is never touched**: knext did not create it
and does not delete it.
</Callout>

## Where to go next

- [Scale to zero](/docs/scale-to-zero) — the autoscaling knobs, and what to change when cold start
  matters more than cost
- [Databases](/docs/databases) — poolers, the writer/reader split, and the typed client
- [Upgrading](/docs/upgrading) — why the operator is upgraded before the CLI
- [Rollback](/docs/rollback) and [Previews](/docs/previews) — the rest of the deploy lifecycle
- [CLI reference](/docs/cli) — every verb and flag
