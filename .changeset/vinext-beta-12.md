---
"@getknext/core": patch
---

New apps scaffolded by `kn-next` now pin vinext `1.0.0-beta.12` (was `1.0.0-beta.11`). The peer ranges vinext declares are unchanged, so existing apps need no other change to upgrade.
