{
  "$schema-note": "Dated + justified allowlist for the vinext PRE-COMPILE CLOSURE supply-chain gate (scripts/precompile-closure-audit.mjs, ADR-0042 C6 / #764). Mirrors security/npm-audit-allowlist.json and the Trivy triage discipline: an accepted HIGH/CRITICAL in the closure that feeds `vite build` is suppressed ONLY by an entry here, so the gate can never be silently neutered. Each entry MUST carry: `id` (a GHSA/CVE id), `justification` (why it is accepted — no fix available, not reachable in our usage, etc.), and `added` (ISO date). `expires` (ISO date) is optional; past its expiry the entry stops suppressing and the risk must be re-justified. `note` (free text) is the only other permitted key: an entry carrying any key outside [id, justification, added, expires, note] makes the gate THROW, because a misspelled `expires` would otherwise be silently ignored and the entry would never expire while reading as though it does. A malformed entry makes the gate THROW rather than be ignored. Bump the dependency FIRST (as nanoid 3.3.17 -> 3.3.18 was, via an override in examples/bun-exec/package.json); allowlist only when there is no fix. SCOPE, stated because it is a real widening: this ONE file backs BOTH closures the gate audits — `--closure examples/bun-exec` and `--app apps/file-manager` (C1/#785) — so an entry accepted for one suppresses the same advisory in the other. Every justification must therefore name the closure and the reachability argument it rests on, and an entry whose argument holds only for one closure must be re-examined, not renewed, if the other starts carrying the same package.",
  "allow": [
    {
      "id": "GHSA-5p2g-fcmc-qvqq",
      "justification": "image-size 2.0.2: JXL and HEIF parsers allow denial of service through infinite loops. NO FIXED VERSION published upstream at the time of acceptance (advisory range <= 2.0.2, first_patched_version null). BE PRECISE ABOUT REACHABILITY, because an earlier draft of this entry got it wrong: image-size is NOT a build-only transitive dependency. It is a DIRECT, exactly-pinned dependency of vinext (\"image-size\": \"2.0.2\" in vinext's package.json) and it is imported SERVER-SIDE at vinext/dist/server/metadata-route-build-data.js, so `resolve.noExternal` inlines it into the compiled binary and it runs in production. Accepted anyway because of WHERE ITS INPUT COMES FROM: the buffer it parses is `fs.readFileSync(route.filePath)` over repo-owned metadata images (icon/opengraph-image and friends) resolved from the app's own source tree at build/serve time — never from request bytes, an uploaded file, or a URL. An attacker who can supply that buffer already commits to the repository. Re-check at expiry: if a fix exists, bump instead of renewing; if metadata-route handling ever reads image bytes from a request, this entry must be dropped, not renewed.",
      "added": "2026-08-19",
      "expires": "2026-11-19"
    },
    {
      "id": "GHSA-w3rx-r6r6-pgpr",
      "justification": "image-size 2.0.2: ICNS parser allows denial of service through an infinite loop. Same package, same version, same posture as GHSA-5p2g-fcmc-qvqq: no fixed version upstream at acceptance, and image-size is a DIRECT dependency of vinext imported server-side (vinext/dist/server/metadata-route-build-data.js), hence inlined into the compiled binary — accepted because the only buffers it parses are `fs.readFileSync` of repo-owned metadata image files, never request data. Re-check at expiry on the same terms.",
      "added": "2026-08-19",
      "expires": "2026-11-19"
    },
    {
      "id": "GHSA-q7rr-3cgh-j5r3",
      "justification": "@opentelemetry/exporter-prometheus 0.208.0 (and @opentelemetry/sdk-node 0.208.0, which re-exports it): the exporter's built-in HTTP server crashes the process on a malformed request. A FIX EXISTS (0.217.0) and the bump is OWED, not waived — see `note`. Accepted for the expiry window because the vulnerable code is never CONSTRUCTED: apps/file-manager wires OTel through `registerOTel` from @vercel/otel (src/instrumentation-node.ts:98) and serves its metrics from a `prom-client` `Registry` on the entry's own metrics listener; grep for `PrometheusExporter` across apps/file-manager returns nothing. The package is in the compiled closure as a transitive module of sdk-node, so it is a real component of the artifact — it just never opens a socket. FIRST FINDING OF THE apps/file-manager CLOSURE GATE (C1/#785): before this gate existed nothing scanned the JS in the pushed image at all, so this advisory was present and invisible. Drop this entry, do not renew it, once the OTel bump lands.",
      "added": "2026-09-04",
      "expires": "2026-10-16",
      "note": "The bump could not be made in the PR that added this gate: it requires regenerating bun.lock (lockfileVersion 3, bun >= 1.4), and CI installs with --frozen-lockfile. Bump @opentelemetry/sdk-node and @opentelemetry/exporter-trace-otlp-grpc in apps/file-manager/package.json to >= 0.217.0 and delete this entry."
    },
    {
      "id": "GHSA-45rx-2jwx-cxfr",
      "justification": "@opentelemetry/propagator-jaeger 2.2.0: denial of service in `JaegerPropagator` via an unhandled exception on a malformed `uber-trace-id` header. A FIX EXISTS (2.9.0), pulled in by the same OTel bump as GHSA-q7rr-3cgh-j5r3 — owed, not waived. Accepted for the expiry window because the propagator is never REGISTERED: the app passes `propagators: ['auto', new CorrelationContextPropagator()]` (src/instrumentation-node.ts:109), which is W3C tracecontext + baggage plus knext's own propagator, and `OTEL_PROPAGATORS` is set nowhere in the repo. Nothing parses a Jaeger header, so no request can reach the throwing path. It is still a component of the compiled binary, which is why it is allowlisted rather than dismissed.",
      "added": "2026-09-04",
      "expires": "2026-10-16",
      "note": "Transitive via @opentelemetry/sdk-node. Resolved by the same bump; delete this entry with GHSA-q7rr-3cgh-j5r3."
    },
    {
      "id": "GHSA-c2c7-rcm5-vvqj",
      "justification": "picomatch 2.3.1: ReDoS via extglob quantifiers in a glob PATTERN. A FIX EXISTS (2.3.2) but it is transitive (via the vite/tailwind/chokidar build toolchain) and pinning it needs a lockfile override, which the PR adding this gate could not regenerate (bun.lock v3 needs bun >= 1.4; CI installs --frozen-lockfile). Accepted for the expiry window on reachability: every picomatch call site in this closure is a BUILD-TIME file matcher whose patterns come from committed config (vite config, tailwind `content`, chokidar watch globs), never from a request. The compiled server does not glob request paths — routing is the vinext/nitro radix matcher, not picomatch. Re-check at expiry: add a `picomatch` override at >= 2.3.2 rather than renewing.",
      "added": "2026-09-04",
      "expires": "2026-10-16"
    }
  ]
}
