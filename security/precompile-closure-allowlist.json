{
  "$schema-note": "Dated + justified allowlist for the vinext PRE-COMPILE CLOSURE supply-chain gate (scripts/precompile-closure-audit.mjs, ADR-0042 C6 / #764). Mirrors security/npm-audit-allowlist.json and the Trivy triage discipline: an accepted HIGH/CRITICAL in the closure that feeds `vite build` is suppressed ONLY by an entry here, so the gate can never be silently neutered. Each entry MUST carry: `id` (a GHSA/CVE id), `justification` (why it is accepted — no fix available, not reachable in our usage, etc.), and `added` (ISO date). `expires` (ISO date) is optional; past its expiry the entry stops suppressing and the risk must be re-justified. `note` (free text) is the only other permitted key: an entry carrying any key outside [id, justification, added, expires, note] makes the gate THROW, because a misspelled `expires` would otherwise be silently ignored and the entry would never expire while reading as though it does. A malformed entry makes the gate THROW rather than be ignored. Bump the dependency FIRST (as nanoid 3.3.17 -> 3.3.18 was, via an override in examples/bun-exec/package.json); allowlist only when there is no fix.",
  "allow": [
    {
      "id": "GHSA-5p2g-fcmc-qvqq",
      "justification": "image-size 2.0.2: JXL and HEIF parsers allow denial of service through infinite loops. NO FIXED VERSION published upstream at the time of acceptance (advisory range <= 2.0.2, first_patched_version null). BE PRECISE ABOUT REACHABILITY, because an earlier draft of this entry got it wrong: image-size is NOT a build-only transitive dependency. It is a DIRECT, exactly-pinned dependency of vinext (\"image-size\": \"2.0.2\" in vinext's package.json) and it is imported SERVER-SIDE at vinext/dist/server/metadata-route-build-data.js, so `resolve.noExternal` inlines it into the compiled binary and it runs in production. Accepted anyway because of WHERE ITS INPUT COMES FROM: the buffer it parses is `fs.readFileSync(route.filePath)` over repo-owned metadata images (icon/opengraph-image and friends) resolved from the app's own source tree at build/serve time — never from request bytes, an uploaded file, or a URL. An attacker who can supply that buffer already commits to the repository. Re-check at expiry: if a fix exists, bump instead of renewing; if metadata-route handling ever reads image bytes from a request, this entry must be dropped, not renewed.",
      "added": "2026-08-19",
      "expires": "2026-11-19"
    },
    {
      "id": "GHSA-w3rx-r6r6-pgpr",
      "justification": "image-size 2.0.2: ICNS parser allows denial of service through an infinite loop. Same package, same version, same posture as GHSA-5p2g-fcmc-qvqq: no fixed version upstream at acceptance, and image-size is a DIRECT dependency of vinext imported server-side (vinext/dist/server/metadata-route-build-data.js), hence inlined into the compiled binary — accepted because the only buffers it parses are `fs.readFileSync` of repo-owned metadata image files, never request data. Re-check at expiry on the same terms.",
      "added": "2026-08-19",
      "expires": "2026-11-19"
    }
  ]
}
