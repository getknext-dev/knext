{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["gateway/**"],
      "outputs": []
    },
    "lint": {
      "inputs": ["gateway/**"]
    },
    "test": {
      "inputs": ["gateway/**", "deploy/**"],
      "outputs": []
    },
    "build:image": {
      "inputs": ["gateway/**"],
      "cache": false
    }
  }
}
