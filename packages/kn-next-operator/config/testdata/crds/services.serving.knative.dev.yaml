# Minimal Knative Service CRD fixture for envtest.
# This is a stripped-down version of the production CRD used only for
# controller unit tests — not for production deployment.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: services.serving.knative.dev
spec:
  group: serving.knative.dev
  names:
    kind: Service
    listKind: ServiceList
    plural: services
    singular: service
    shortNames:
      - ksvc
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
