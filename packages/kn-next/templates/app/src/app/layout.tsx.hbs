import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "{{ name }}",
  description: "Scale-to-zero Next.js app generated by `kn-next create`",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
