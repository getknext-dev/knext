/**
 * Re-export the framework's cache handler.
 *
 * Generated by `kn-next create` / `turbo gen zone` — shared VERBATIM between
 * both scaffolders (create-scaffold-parity.test.ts).
 *
 * The implementation lives in @getknext/core/adapters/cache-handler (Redis,
 * with an in-memory fallback when REDIS_URL is unset). This file exists only
 * because Next.js takes `cacheHandler` as a file PATH — keeping the logic in
 * the framework means bug fixes reach every app without touching this file.
 */
export { default } from "@getknext/core/adapters/cache-handler";
