/**
 * GENERATED BY the knext app scaffolder — `knext create` (standalone-by-
 * default target, ADR-0058/#1183/#1342).
 *
 * Re-export of the package-shipped knext NextAdapter. The adapter
 * implementation lives in `@getknext/core` so the official Next.js
 * compatibility harness can point arbitrary fixture apps at it via
 * `NEXT_ADAPTER_PATH`. This file is a thin re-export — no behavior of its
 * own. `next.config.ts` wires it through the top-level `adapterPath` config
 * (graduated out of `experimental` at next 16.2).
 *
 * Only used on the standalone (`build: 'turbopack'`/`'webpack'`) target —
 * vinext (`build: 'vinext'`) is Vite/rolldown and never calls Next's
 * webpack/turbopack adapter hooks.
 */

import adapter from "@getknext/core/adapter";
import type { NextAdapter as _NextAdapter } from "next";

// Compile-time assertion that the package adapter conforms to the official
// shape, without forcing a cross-instance assignment on the exported value.
export type KnextAdapter = _NextAdapter;

export default adapter;
