{
  "name": "knext-standalone-drain-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal Next.js output:'standalone' app used ONLY by standalone-drain.docker-e2e.test.ts to prove SIGTERM drain under the shipped standalone-on-bun image. NOT a workspace member (its deps are installed into a throwaway build dir by the e2e), so it never inherits or pollutes the root lockfile.",
  "scripts": {
    "build": "next build"
  },
  "dependencies": {
    "next": "16.3.3",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
